###################
### KEYBINDINGS ###
###################

# Lines ending with `# [hidden]` won't be shown on cheatsheet
# Lines starting with #! are section headings

# See https://wiki.hypr.land/Configuring/Keywords/
$mod = SUPER # Sets "Windows" key as main modifier

#!
##! Shell

##! Utils

##! Window
bind = $mod, Q, killactive, # Close

##! Workspace
# Switching

#/# bind = Super, Hash,, # Focus workspace # (1, 2, 3,...)
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

# Move focus with mod + arrow keys
bind = $mod, left, movefocus, l
bind = $mod, right, movefocus, r
bind = $mod, up, movefocus, u
bind = $mod, down, movefocus, d

# Move active window to a workspace with mod + SHIFT + [0-9]
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10

##! Session

##! Media

##! Apps
bind = $mod, RETURN, exec, $terminal # Terminal
bind = $mod, W, exec, $browser # Browser
bind = $mod, E, exec, command -v $fileManager && $terminal -1e $fileManager # File manager
bind = $mod, M, exec, Telegram # Telegram
bind = $mod, C, exec, command -v $editor && $terminal -1e $editor # Code editor
bind = CTRL+SHIFT, ESCAPE, exec, command -v $systemMonitor && $terminal -1e $systemMonitor # System monitor
